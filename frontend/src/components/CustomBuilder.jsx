import React, { useState } from "react";
import { DragDropContext, Droppable, Draggable } from "@hello-pangea/dnd";

export default function CustomIdBuilder({ template, onChange }) {
  // template: [{id:'a1', type:'fixed', value:'ITM-'} ...]
  // implement reorder, add, delete, edit element
  return <div>/* DnD UI here */</div>;
}
